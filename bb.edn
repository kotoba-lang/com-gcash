{:paths ["src" "test"]
 :tasks {test {:doc "Run the cljc contract + behavioral test under babashka"
               :extra-paths ["test"]
               :requires ([gcash.main-test])
               :task (let [{:keys [fail error]} (clojure.test/run-tests 'gcash.main-test)]
                       (System/exit (if (pos? (+ fail error)) 1 0)))}}}
